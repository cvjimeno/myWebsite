---
// src/components/blog/ReadingProgressBar.astro (IMPROVED VERSION)
const { class: className } = Astro.props;
---
<!-- 
  THE FIX: We use class:list to merge the component's default classes 
  with any extra classes passed in (like `lg:hidden`).
-->
<div id="progress-container" class:list={[
  "fixed top-16 left-0 w-full h-[3px] z-40 bg-transparent",
  className
]}>
  <div 
    id="progress-bar" 
    class="h-full bg-[var(--color-accent-highlight)]" 
    style="width: 0%;"
  ></div>
</div>