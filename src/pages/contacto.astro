---
// src/pages/contacto.astro (FINAL, TAILWIND v4-CORRECTED)
import BaseLayout from '../layouts/BaseLayout.astro';
import { Icon } from 'astro-icon/components';
import SocialIcon from '../components/common/SocialIcon.astro';

const socialLinks = [
  { href: "https://x.com/cvjimeno", name: "x", label: "X / Twitter" },
  { href: "https://linkedin.com/in/cvjimeno/", name: "linkedin", label: "LinkedIn" },
];
---
<BaseLayout 
  title="Contacto"
  description="¿Tienes una idea, una pregunta o una propuesta de colaboración? Hablemos. Encuentra aquí las mejores formas de ponerte en contacto conmigo."
>
  <div class="py-20 md:py-24">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <header class="text-center mb-12">
        <div class="inline-flex items-center justify-center w-16 h-16 bg-accent-primary-bg dark:bg-accent-primary/20 rounded-full mb-6">
          <Icon name="lucide:mail" class="w-8 h-8 text-accent-primary"/>
        </div>
        <h1 class="text-4xl md:text-5xl font-bold text-text-primary mb-4">
          Hablemos
        </h1>
        <p class="text-xl text-text-secondary max-w-2xl mx-auto leading-relaxed">
          ¿Tienes una idea interesante? ¿Quieres colaborar en algo? 
          Me encanta conocer nuevas perspectivas y proyectos.
        </p>
      </header>

      <div class="max-w-3xl mx-auto">
        <form class="space-y-6 bg-bg-secondary p-8 md:p-10 rounded-xl shadow-lg border border-border-card">
          
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
            <div>
              <label for="name" class="block text-sm font-medium text-text-secondary mb-2">Nombre</label>
              <!-- THE FIX: All styles are now directly in the class attribute -->
              <input 
                type="text" 
                id="name" 
                name="name" 
                class="w-full rounded-lg border border-border-card bg-bg-secondary px-4 py-3 text-text-primary transition-all duration-200 placeholder:text-text-secondary/60 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-accent-primary dark:bg-bg-primary" 
                placeholder="Tu nombre" 
              />
            </div>
            <div>
              <label for="email" class="block text-sm font-medium text-text-secondary mb-2">Email</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                class="w-full rounded-lg border border-border-card bg-bg-secondary px-4 py-3 text-text-primary transition-all duration-200 placeholder:text-text-secondary/60 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-accent-primary dark:bg-bg-primary" 
                placeholder="tu@email.com" 
              />
            </div>
          </div>
          
          <div>
            <label for="subject" class="block text-sm font-medium text-text-secondary mb-2">Asunto</label>
            <input 
              type="text" 
              id="subject" 
              name="subject" 
              class="w-full rounded-lg border border-border-card bg-bg-secondary px-4 py-3 text-text-primary transition-all duration-200 placeholder:text-text-secondary/60 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-accent-primary dark:bg-bg-primary" 
              placeholder="Propuesta de colaboración" 
            />
          </div>

          <div>
            <label for="message" class="block text-sm font-medium text-text-secondary mb-2">Tu mensaje</label>
            <textarea 
              id="message" 
              name="message" 
              rows="6" 
              class="w-full rounded-lg border border-border-card bg-bg-secondary px-4 py-3 text-text-primary transition-all duration-200 resize-none placeholder:text-text-secondary/60 focus:border-transparent focus:outline-none focus:ring-2 focus:ring-accent-primary dark:bg-bg-primary" 
              placeholder="Cuéntame tu idea, pregunta o propuesta..."
            ></textarea>
          </div>

          <div class="pt-2 text-right">
            <button 
              disabled 
              class="w-full sm:w-auto px-8 py-3 font-semibold rounded-lg cursor-not-allowed bg-bg-secondary text-text-secondary/70 border border-border-card dark:bg-bg-primary dark:text-text-secondary/50 dark:border-border-subtle"
            >
              <span class="flex items-center justify-center gap-2">
                <Icon name="lucide:send" class="w-4 h-4"/>
                <span>Enviar mensaje</span>
              </span>
            </button>
            <p class="text-xs text-text-secondary mt-3 text-left sm:text-right">* Formulario próximamente disponible.</p>
          </div>
        </form>
      </div>

      <div class="mt-16 text-center">
        <h3 class="font-semibold text-text-primary mb-4">O encuéntrame aquí:</h3>
        <div class="flex justify-center gap-4">
          {socialLinks.map(social => (
            <a 
              href={social.href} 
              target="_blank" 
              rel="noopener noreferrer"
              aria-label={social.label}
              class="w-12 h-12 bg-bg-card hover:bg-accent-primary-bg dark:hover:bg-accent-primary/10 rounded-lg flex items-center justify-center transition-all duration-200 hover:scale-105 group border border-border-card"
            >
              <SocialIcon name={social.name} class="w-5 h-5 text-text-secondary group-hover:text-accent-primary"/>
            </a>
          ))}
        </div>
      </div>
      
    </div>
  </div>
</BaseLayout>

<!-- THE FIX: The entire <style> block has been removed -->