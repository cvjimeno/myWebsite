---
// src/components/sections/HeroSection.astro
import { Image } from 'astro:assets';
import heroImageCarlos from '../../assets/images/hero-main-picture.webp';
import Button from '../common/Button.astro';
---

<section class="hero py-16 md:py-24 px-4 sm:px-6 lg:px-8" aria-labelledby="hero-heading">
  <div class="container mx-auto">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-12 md:gap-16 items-center">
      
      <figure class="order-1 md:order-none">
        <Image 
          src={heroImageCarlos} 
          alt="Carlos Velasco - Foto de perfil con efecto pincelada"
          widths={[400, 600, 800]}
          sizes="(max-width: 768px) 100vw, 50vw" 
          format="webp"
          class="w-full max-w-2xl mx-auto md:mx-0 rounded-lg" 
        />
      </figure>

      <header class="text-center md:text-left order-2 md:order-none">
        <h1 id="hero-heading" class="text-5xl sm:text-6xl md:text-7xl lg:text-8xl font-bold text-text-primary leading-tight tracking-tighter mb-6">
          ¡Hola! <span class="inline-block animate-wave">👋</span>
        </h1>
        <p class="text-lg md:text-xl text-text-secondary mb-8 leading-relaxed max-w-xl mx-auto md:mx-0">
          Soy Carlos Velasco, un científico apasionado de la ciencia y la tecnología... y que escribe a ratos sobre Inteligencia Artificial. ¡Bienvenidos a mi hogar digital!
        </p>
        <nav class="flex flex-col sm:flex-row justify-center md:justify-start gap-4" aria-label="Primary hero navigation">
          <Button href="/#about-me-snapshot" variant="primary">
            Conóceme
          </Button>
          <Button href="/blog" variant="secondary">
            Mi Blog
          </Button>
        </nav>
      </header>
    </div>
  </div>
</section>

